[package]
name = "wakectl"
version = "0.1.0"
edition = "2024"

[dependencies]
clap = { version = "4", features = ["derive"] }

chrono = "0.4"
chrono-tz = "0.9"

serde = { version = "1", features = ["derive"] }

toml = "0.8"

anyhow = "1"

directories = "5"
